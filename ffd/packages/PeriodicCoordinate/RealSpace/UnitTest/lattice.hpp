namespace ffd::periodic_coordinate::unit_test{

  void lattice_real_space(){

    // PeriodicCoordinate<int, 3> x;
    // x.RealSpaceVectors = {{1., -1, 2}};

    // x.Variable = 2;
    // auto delta_x = RealSpace(x);
    // for(int j=0; j < 3; ++j){
    //   delta_x[j] -= 2*((j==0)-(j==1)+(j==2)*2);
    //   assert( std::abs(delta_x[j]) <
    // 	      10*std::numeric_limits<Real>::epsilon() );
    // }

  }
  

}//namespace
