namespace ffd::periodic_coordinate::unit_test{

   void atomic_real_space(){
  //   PeriodicCoordinate<int, 2> atomic;
  //   atomic.LowerUpperBound = {0, 2};
  //   atomic.RealSpaceVectors = {{1., 0.}, {0., 1.}};

  //   atomic.Variable = 0;
  //   auto delta_at = RealSpace(atomic);
  //   for(int j=0; j < 2; ++j){
  //     delta_at[j] -= (j==0);
  //     assert( std::abs(delta_at[j]) < 10*std::numeric_limits<Real>::epsilon() );
  //   }
    
  //   atomic.Variable = 1;
  //   delta_at = RealSpace(atomic);
  //   for(int j=0; j < 2; ++j){
  //     delta_at[j] -= (j==1);
  //     assert( std::abs(delta_at[j]) < 10*std::numeric_limits<Real>::epsilon() );
  //   }
   }

}//namespace
