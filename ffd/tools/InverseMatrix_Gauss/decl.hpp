namespace ffd::inverse_matrix_gauss{

  // template<typename matrix_t>

  // std::pair<std::decay<matrix_t>::type,
  // 	    typename std::decay<decltype(std::declval<matrix_t>()[0])>::type>
  // Inverse_and_Determinant(matrix_t&& A);
	
}//namespace
